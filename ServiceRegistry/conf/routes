PUT /registry/service/:name/info      registry.controllers.RegistryController.registerServiceInfo(name: String)
GET /registry/service/:name/info      registry.controllers.RegistryController.getServiceInfo(name: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
