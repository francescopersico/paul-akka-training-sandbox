POST /agilepm/products             agilepm.controllers.ProductsController.createProduct()
GET  /agilepm/products/:id         agilepm.controllers.ProductsController.product(id: String)

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
